#include<bits/stdc++.h>
using namespace std;
int main(){
    long long t;
    cin >> t;
    while (t--) {
        long long a, b, c, d;
        cin >> a >> b >> c >> d;

        // Choosing x = b, y = c, z = c
        cout << b << " " << c << " " << c << endl;
    }
    return 0;
}
